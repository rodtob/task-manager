# Vite React Boilerplate

A lightweight **Vite** boilerplate for React with **Tailwind CSS** and **Vitest** pre-configured.

## Requirements

- Node.js v18+
- npm v9+ (or yarn/pnpm)

## Setup for development

npm install

npm run dev






